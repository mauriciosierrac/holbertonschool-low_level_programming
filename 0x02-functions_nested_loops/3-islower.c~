#include "holberton.h"


/**
* _islower - check if some alpha is lower
*
* Return: 0 or 1
*
* c : parameter of 3-main.c
**/

int _islower(int c)
{

if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
