#include "holberton.h"

/**
 *
 *
 *
 *
 **/

void reverse_array(int *a, int n)
{
  int x, y, z;

  z = 0;

  for (x = n - 1; x >= (n / 2); x--)
    {
      y = a[z];
      a[z] = a[x];
      a[x] = y;
      z++;
    }
   
  }
