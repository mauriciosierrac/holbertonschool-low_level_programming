#include "holberton.h"
#include <string.h>
#include <stdio.h>



/**
 * _strlen - return length of a string
 *
 * @s: variable pointer
 **/

int _strlen(char *s)
{

int y = 0;

while (s[y] != '\0')
{
y++;
}
return (y);
}
