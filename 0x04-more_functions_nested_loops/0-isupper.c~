#include "holberton.h"
/**
 * _isupper - check if char is upper
 *@res: variable for check
 *Return: the variable for check
 **/

int _isupper(int c)
{
int res;
if (c >= 'A' && c <= 'Z')
{
res = 1;
}
else 
{
res = 0;
}

return(res);
}
